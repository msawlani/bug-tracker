import { FilterBugsPipe } from './filter-bugs.pipe';

describe('FilterBugsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBugsPipe();
    expect(pipe).toBeTruthy();
  });
});
